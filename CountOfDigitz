#include <stdio.h>
int main() {
    int n, rem, sum = 0;
    printf("\nEnter a number:");
    scanf("%d", &n);
    while (n!=0)
    {
        rem=n%10;
        sum = sum + rem/rem;
        n = n/10;
    }
    printf (" count of digits is %d", sum);

    return 0;
}